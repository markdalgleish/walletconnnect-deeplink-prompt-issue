# WalletConnect Deep Link Prompt Issue

Minimal reproduction of an issue where multiple attempts to send a transaction fails to automatically deep link into the wallet app on iOS.

## Running locally

```bash
pnpm install
pnpm dev
```
